# hardcore_-automation-
Automation of completely deleting the current world and generating a new world on power stack server with random server, with the same instance..

Just make a .env file and add EMAIL and PASSWORD

Then you can either run streamlit run app/main.py
Or, docker compose up and you can see the thing in web browser, make sure to click on start monitoring each time, its using playwright.